<UserControl x:Class="lab12.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lab12.Views"
             mc:Ignorable="d">
    <StackPanel Margin="20">
    <TextBlock Text="Редактирование контакта" FontSize="18" FontWeight="Bold"/>

    <TextBlock Text="Имя" Margin="0,10,0,5"/>
    <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"/>

    <TextBlock Text="Телефон" Margin="0,10,0,5"/>
    <TextBox Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}"/>

    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
        <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="5"/>
        <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="5"/>
    </StackPanel>
    </StackPanel>
</UserControl>
