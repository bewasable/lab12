<UserControl x:Class="lab12.Views.ContactsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lab12.Views"
             mc:Ignorable="d">
    <Grid Margin="10">
    <StackPanel>
        <TextBlock Text="Имя"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Телефон" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />

        <Button Content="Добавить" Command="{Binding AddCommand}" Margin="0,10,0,10"/>

        <DataGrid ItemsSource="{Binding Contacts}"
                      SelectedItem="{Binding SelectedContact, Mode=TwoWay}"
                      AutoGenerateColumns="False" Height="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Редактировать" Command="{Binding EditCommand}" Margin="0,0,10,0"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}"
                        CommandParameter="{Binding SelectedContact}"/>
        </StackPanel>
    </StackPanel>
    </Grid>
</UserControl>
